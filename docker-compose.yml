version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: meu_banco_mysql
    environment:
      MYSQL_ROOT_PASSWORD: senha_raiz_super_segura
      MYSQL_DATABASE: sistema_db
      MYSQL_USER: usuario_treinamento
      MYSQL_PASSWORD: senha_segura123
    ports:
      - "3306:3306"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql